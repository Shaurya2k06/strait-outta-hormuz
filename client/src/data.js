import dashboardData from './dashboard-data.json'

export const {
  actions,
  cells,
  concentration,
  controls,
  customers,
  frontier,
  heldQueue,
  metadata,
  routes,
  scenarios,
  scoreRobustness,
  scoreRows,
} = dashboardData
